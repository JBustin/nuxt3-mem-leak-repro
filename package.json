{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "inspect": "npm run build && node --inspect .output/server/index.mjs",
    "load-test": "artillery run load-tests/home.yml",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^3.14.1592",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "artillery": "^2.0.21"
  }
}
